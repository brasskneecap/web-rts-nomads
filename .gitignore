# Node
node_modules/
dist/
.env

# Go
bin/
*.exe
*.out

# air live-reload build artifacts (api.exe, api.exe~ backup, build-errors.log).
# Regenerated on every save; nothing here belongs in the repo.
server/tmp/

# OS
.DS_Store

# Logs
*.log

# IDE
.vscode/
settings.local.json

# PixelLab per-frame unit exports — packed into sprites.json + packed/ strips
# by `npm run pack:sprites`. The raw frames aren't imported by the runtime.
client/src/game-portal/src/assets/*/*/animations/
