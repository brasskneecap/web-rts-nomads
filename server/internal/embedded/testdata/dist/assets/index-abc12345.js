// nomads spa test fixture — fingerprinted asset used by internal/embedded tests
// to assert that files under assets/ receive an immutable, long-lived cache header.
console.log("nomads spa test fixture");
